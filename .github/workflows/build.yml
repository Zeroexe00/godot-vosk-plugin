name: Build GDExtension for Linux (multi-arch)

on:
  push:
    branches: [release]

jobs:
  build-multiarch:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        arch:
          - x86_64
          - aarch64
          - arm

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          submodules: 'recursive'

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y python3 python3-pip curl tar pkg-config
          pip install scons==4.8.1

      - name: Download and extract Buildroot SDK for ${{ matrix.arch }}
        run: |
          ARCH=${{ matrix.arch }}
          if [ "$ARCH" = "arm" ]; then
            SDK_FILE="arm-godot-linux-gnueabihf_sdk-buildroot.tar.bz2"
            SDK_DIR="arm-godot-linux-gnueabihf_sdk-buildroot"
          else
            SDK_FILE="${ARCH}-godot-linux-gnu_sdk-buildroot.tar.bz2"
            SDK_DIR="${ARCH}-godot-linux-gnu_sdk-buildroot"
          fi
          curl -LO https://github.com/godotengine/buildroot/releases/download/godot-2023.08.x-4/$SDK_FILE
          tar xf $SDK_FILE
          rm -f $SDK_FILE
          cd ${SDK_DIR}
          ./relocate-sdk.sh

      - name: Build with SCons for ${{ matrix.arch }}
        run: |
          ARCH=${{ matrix.arch }}
          GODOT_SDK_LINUX_X86_64=/${pwd}/x86_64-godot-linux-gnu_sdk-buildroot
          GODOT_SDK_LINUX_X86_32=/${pwd}/i686-godot-linux-gnu_sdk-buildroot
          GODOT_SDK_LINUX_ARM64=/${pwd}/aarch64-godot-linux-gnu_sdk-buildroot
          GODOT_SDK_LINUX_ARM32=/${pwd}/arm-godot-linux-gnueabihf_sdk-buildroot
          $CXX --version
          ls -la
          if [ "$ARCH" = "arm" ]; then
            SDK_DIR="arm-godot-linux-gnueabihf_sdk-buildroot"
            SDK_SUBMODULE_DIR="arm-godot-linux-gnueabihf_sdk-buildroot/arm-godot-linux-gnueabihf"
            ARCH_GODOT="arm64"
          elif [ "$ARCH" = "aarch64" ]; then
            SDK_DIR="${ARCH}-godot-linux-gnu_sdk-buildroot"
            SDK_SUBMODULE_DIR="${ARCH}-godot-linux-gnu_sdk-buildroot/${ARCH}-godot-linux-gnu"
            ARCH_GODOT="x86_64"
          else
            SDK_DIR="${ARCH}-godot-linux-gnu_sdk-buildroot"
            SDK_SUBMODULE_DIR="${ARCH}-godot-linux-gnu_sdk-buildroot/${ARCH}-godot-linux-gnu"
            ARCH_GODOT="${ARCH}"
          fi
          pwd
          scons platform=linux target=template_release arch=$ARCH_GODOT

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: gdextension-linux-${{ matrix.arch }}
          path: bin/linux/${{ matrix.arch }}/